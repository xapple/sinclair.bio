---
// Root index page — Astro uses this to generate the redirect from / to /en/
// With prefixDefaultLocale: true, this page is never rendered directly.
// Astro automatically creates a redirect to the default locale.
---
