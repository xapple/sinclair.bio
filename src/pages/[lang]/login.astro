---
import AuthLayout from '../../layouts/AuthLayout.astro';
import { langStaticPaths } from '../../i18n/page';

export const getStaticPaths = langStaticPaths;

const { lang, t } = Astro.locals;
---

<AuthLayout kind="login" action="https://clients.sinclair.bio/login">
  <div class="field">
    <label for="email">{t('login:email-label')}</label>
    <input
      id="email"
      name="email"
      type="email"
      autocomplete="username"
      placeholder={t('login:email-placeholder')}
      required
    />
  </div>
  <div class="field">
    <label for="password">{t('login:password-label')}</label>
    <input
      id="password"
      name="password"
      type="password"
      autocomplete="current-password"
      placeholder={t('login:password-placeholder')}
      required
    />
  </div>

  <div class="form-options">
    <label class="checkbox-label">
      <input type="checkbox" name="remember" value="true" />
      {t('login:remember')}
    </label>
    <a href={`/${lang}/forgot`} class="forgot-link">{t('login:forgot')}</a>
  </div>

  <Fragment slot="footer">
    <span style="white-space: nowrap">{t('login:client-only-1')}</span>
    {' '}
    <span style="white-space: nowrap">{t('login:client-only-2')}</span>
  </Fragment>
</AuthLayout>
