---
import Logo from './topbar/Logo.astro';
import NavLinks from './topbar/NavLinks.astro';
import ThemeToggle from './topbar/ThemeToggle.astro';
import LanguageSwitcher from './topbar/LanguageSwitcher.astro';
import Icon from './Icon.astro';

const { lang, t } = Astro.locals;
---

<header class="site-topbar sticky top-0 z-50 w-full bg-bar border-b border-white/25">
  <nav class="mx-auto flex h-16 max-w-7xl items-center justify-between px-4 sm:px-6 lg:px-8">
    <!-- Left: Logo -->
    <div class="flex shrink-0 items-center">
      <Logo />
    </div>

    <!-- Center: Nav links (hidden on mobile) -->
    <div class="hidden md:flex md:items-center md:gap-1">
      <NavLinks />
    </div>

    <!-- Right: Widgets + Hamburger -->
    <div class="flex items-center gap-3 md:gap-6">
      <div class="flex items-center"><LanguageSwitcher /></div>
      <div class="flex items-center"><ThemeToggle /></div>

      <!-- Login button -->
      <a
        href={`/${lang}/login`}
        class="bar-icon-btn md:ml-2"
        aria-label={t('a11y:login')}
      >
        <Icon name="person-circle" size="md" class="md:w-8 md:h-8" />
      </a>

      <!-- Hamburger (mobile only). Both glyphs render; CSS picks one based on
           the menu's data-closed state. Menu starts open, so the close (X)
           glyph is the visible one initially. -->
      <button
        id="mobile-menu-toggle"
        type="button"
        class="mobile-menu-toggle bar-icon-btn md:hidden"
        aria-expanded="true"
        aria-label={t('a11y:toggle-menu')}
      >
        <Icon name="square-x" size="md" class="mobile-menu-icon-open" />
        <Icon name="square-menu" size="md" class="mobile-menu-icon-closed" />
      </button>
    </div>
  </nav>

  <!-- Mobile menu (open by default, dismissible via hamburger).
       Open/close animation lives in global.css under #mobile-menu. -->
  <div id="mobile-menu" class="mobile-menu border-t border-white/25 md:hidden">
    <div class="flex justify-center gap-1 px-2 py-2">
      <NavLinks />
    </div>
  </div>
</header>

<script>
  import { initMobileMenu } from '../scripts/mobile-menu';
  initMobileMenu();
</script>
